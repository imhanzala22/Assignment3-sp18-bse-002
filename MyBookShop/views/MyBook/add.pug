extends ../layout

block content
    h1 Add New Product
    form(action="/MyBook/add",method="post")
        .form-group
            label(for="name") BOOK Name
            input#name(type="text", class="form-control", name="name")
        .form-group
            label(for="price") BOOK Price
            input#price(type="text", class="form-control", name="price")
        input(type="submit", class="btn btn-info")